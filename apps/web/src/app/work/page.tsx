import { WorkQueuePage } from '@/features/work/pages/WorkQueuePage';

export const metadata = {
  title: 'Operational Work | ArogyaGrid',
};

export default function WorkPage() {
  return <WorkQueuePage />;
}